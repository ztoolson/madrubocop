require "spec_helper"

describe Madrubocop do
  it "has a version number" do
    expect(Madrubocop::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
